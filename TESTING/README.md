# How to Test
```
# Create virtual environment
python3.6 -m venv venv 
# Activate it
source venv/bin/activate
# Install the package with pip 
pip install py-msci-esg --upgrade
# execute test script (modify it if you want)
python test.py
```